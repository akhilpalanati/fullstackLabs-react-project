import React from 'react';
import { Typography } from "@material-ui/core";

const BlocksNotFound = () => {
  return (
    <div>
      <Typography component= 'h4'>
        Error While Fetching Blocks
      </Typography>
    </div>
  );
};

export default BlocksNotFound;
